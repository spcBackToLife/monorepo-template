- cursor 调用 mcp 后，前端页面是不是得收到 websocket 消息更新页面还是怎么弄？
- 还可以提供 canvas 的设计器，能设计各种 svg 小图标等等，将这个图标做成资产放在项目下，同时项目下应该还可以配置很多图片素材、视频、pag等格式的资源
- 我们有了完整的 schema 可以 react 还原整个页面，那也应该有 mock 数据管理，来模拟各种流程下的表现交互？并且能实现各种状态下截图保存或者就是不同数据的排列组合，从而我们既可以有真实的 schema 交互，又可以有各种状态下的全景细节？
- 是否可以结合：https://github.com/alibaba/page-agent 这个在功能完成后，让 ai 做自动化流程测试？
- 右击某个组件可以做细节素材设计，如：拼装的背景、插入素材等，准备好素材后可以展示出来
- 我觉得资产有两种：
  - 只沉淀最基础的交互，如：输入框、下拉框、按钮等，UI 等细节没有
  - 沉淀业务风格的基础组件


# 编辑器设计
结合第一性原理，帮我设计下整个渲染区域：
1. 渲染区域肯定是基于 schema 渲染出来的，在选中节点后，右侧可以对节点做各种样式设计、交互设计、状态设计
2. 渲染区的元素都应该是相对于父级的相对位置，在拖动的时候可以精准放下，类似于 figma 的体验
3. 画的每一个元素比如：span、div 或者组件等，天然就具备一些 props 无论是文字还是样式，尤其是沉淀组件的时候需要标准化暴露 props，表示这个组件有哪些是可以配置的、调整的，不允许调整的不暴露，这样的话，真正在渲染区域渲染就需要传递这些 props 得有一个地方配置？
另外，如果组件在不同状态下，props 传递的应该不一样，也应该支持，比如：一个任务在进行中、已完成等等，页面状态或者组件会有差别？这里就有另一个问题，就是对于设计页面来说有全局状态，也有组件状态，全局就是整个页面通用比如任务状态，多个组件都会受到影响而有不一样的 props，局部就是某个组件的不同状态，比如：按钮的 loading 状态，输入框的错误提示状态，正常状态等等，另外还具备一些通用的状态如：悬浮、点击、正常等等
4. 如：design_docs/images/底部工具栏.png，我们也应该设计一个这样的工具栏，包括：选中元素的那个箭头，可以选择不同元素放到画布里，尤其是方块 div，可以像 figma 点击一样画，然后放到对应的位置，注意所有的操作都得先选中父级容器才可添加

整体编辑器的目标就是：
1. 我可以基于 schema 渲染的东西看到页面
2. 我可以对渲染的页面做非常详细高效的交互，比如：调整某个元素的细节等等
3. 我们这个渲染器和其他的不一样的是，它有数据管理，状态管理（全局、局部等），就像真实的组件一样，这样对于数据的维护、状态的调整管理会比 figma 更灵活更便捷，更高效的看到各种状态下的 UI 表现，也可以结合这些数据定义不同状态下 UI 展示成什么样子，随时可以从全局和局部切换状态来看样式细节和针对性的样式设计。这是一个突破性的创新

这个编辑器应该有很多详细的产品设计内容：
- 整体的编辑器如何交互
- 组件、资产怎么管理
- 单个组件的状态交互如何管理
- 单个组件的静态资源如何管理和编辑，比如：svg 插入图片等
- 整体页面的状态如何管理和操作，包括全局状态等等
- 如何设计完成后就像一个真实的软件一样可以交互
- 工具栏如何设计
- 右侧配置面板如何设计，如：样式、交互、状态、数据等等

结合第一性原理帮忙分析和设计下，注意合理的拆分设计计划，拆分文档目录，对每个模块针对性的提出问题和分析设计，整合下之前的设计，拆分最后沉淀到：design_docs/02-product



# 最根本目标
我想设计即产物：
- 无需对数据做组件分类，交互分析等等，天然就可以轻松转化成企业级代码，包括：代码组织、逻辑抽象、完整交互
- AI 可以通过 mcp 来根据用户需求绘制和调整 UI，不是从页面级别的从0-1 生成，而是通过操作一步步可以把 UI 画出来，可灵活调整和撤销任意步骤任意模块，不像现在的设计软件，每次对话调整都是重新生成。
- 同时对设计师友好，具备组件资产沉淀，基于交互的设计，即不是画一堆 frame 反复解释如何交互，设计的过程中就是正常的软件交互，结合数据mock能力还可以快捷的设计各种状态下的呈现形态和逻辑
- 对产品友好，设计即需求表达，根据设计数据，轻易的可以转化成需求文档，产品所有的交互链路清清楚楚，无需大模型做任何深度分析。



# 状态触发点
从第一性原理来思考，我们可以自定义状态，如：登陆成功等，但是这些状态都有触发点，
比如：
- 从 api 接口返回结果里设置不同状态？不同 mock 结果不同状态
- 点击一些按钮，直接切换状态？
还有什么其他的触发点也可以切换状态

另外所有的元素都应该可以默认在所有状态里，除非特定配置选择某个状态，则只有某个状态展示

所以在这里似乎存在着：新增的状态，如何管理和设计触发点？

从资深的产品设计思考下，如何设计这块内容，以及技术方案？看看当前的设计合理么？通用么，好扩展么，符合开闭么？


# 设计稿和代码的双向转换引擎
设计稿转代码遇到了几个重大问题：
- schema 的设计理论上可以转换成：vue、react 等不同代码
- 理论上大部分工作可以直接通过脚本完成转换，少部分工作交给 AI 识别后转换。
- 转换后的组件或者页面应该保留转换时候的 schema 版本，下次 schema 有更新后通过 diff 引擎识别出变更节点后可以基于 diff 做组件更新或者增量迭代，这部分也可以交给 AI
- 设计稿里的全局状态、api 请求等数据状态管理可以深度结合 mobx 等状态库做标准化封装，从而可以双向转换？
- 无论是 react 还是 mobx 状态、api 定义等等场景的转换，都可以做成 adapters 层，相似的转换流程和策略，可以插拔的转化成不同的代码，可以天然支持增量迭代和双向转换。
  - 比如代码有所改动，也可以有什么方式同步到设计稿上么？？还是这块怎么做，onlook 是怎么做的？
结合第一性原理和资深前端研发工程师，帮我思考下：状态、请求、组件、前端 mvc 等等机制，如何设计通用的双向转换引擎，能够做到 100% 的转换，即便是使用 AI 的核心价值也只是一些
语义识别后的标准化 JSON 输出转换，然后结合我们的转换引擎实现代码的 100% 转换？这个要怎么谁？
甚至可以思考要不要做这么复杂，我们已经有可以渲染成react 的引擎了，未来优化这个引擎，是不是都不需要人为去动代码了，都是通过操作来改代码，这样 schema 就是唯一桥梁和数据源，就不需要
转换来转换去？

# 素材编辑器 ✅ 已完成方案设计
> 完整方案见：[design_docs/02-product/editor/12-material-editor/README.md](./02-product/editor/12-material-editor/README.md)

- 素材存粹是一个非常强大的编辑器
- 素材需要承载容器，可以是：div、span、img 也可以是样式属性：background ？
- 素材编辑器应该是一个独立的模块和能力？
- 素材设计好了之后和 schema 数据放一起么，图片存哪儿呢？是不是要有 s3 还是暂时怎么存储在服务端？测试的时候就是通过接口上传到服务器端，放在服务器指定目录下吧？后续上传到 s3？
- 目前有什么开源的绘图比较厉害的项目么？还是我们得从 0 到 1 自己画，像 figma 一样的：直线、折线、曲线、圆角、圆、椭圆、方形等等各种形状的编辑器？
- 素材编辑器要支持做动画么？有开源的做 gif 和动画素材 pag 等之类的编辑器么？
- 结合第一性原理分析下，如何做一个非常强大的素材编辑器
- 结合目前的项目，看看怎么把素材编辑器的能力和我们的设计编辑器结合起来，给我做一个素材编辑器的方案设计


