# 实战示例（按需加载）

参考端到端走完一个模块的样板。学样板而不是抄。

## 文件清单

| 文件 | 内容 |
|------|------|
| `login-module.md` | 登录模块完整五步法（A 用户故事 → B 流程 → C 规则 → D 数据 → E 骨架）+ MCP 调用样板 |

## 何时加载

- 不熟悉某个 plan 任务怎么做时 → 看本目录下对应模块的样板
- 不必须，参考性质。**模板（`note-templates/`）是必填骨架，示例（`examples/`）只是真实展开示意**
