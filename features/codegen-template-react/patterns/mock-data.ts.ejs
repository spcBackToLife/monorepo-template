/**
 * Mock Data Configuration — AUTO-GENERATED by design-codegen
 *
 * This file is generated from the design schema's ApiDataSource.mock config.
 * DO NOT EDIT manually — changes will be overwritten on next codegen run.
 *
 * Structure:
 *   - mockConfigs: Record<dataSourceId, MockConfig>
 *     - scenarios: MockScenario[]
 *     - activeScenarioId: string (default from schema)
 *   - endpointMap: Record<dataSourceId, urlPath> — for URL matching in interceptor
 *
 * @see /features/design-schema/src/types/dataSource.ts — MockScenario / MockConfig
 */

import { initMock, _setEndpointMap } from '@/utils/mock-manager';

// ─── Mock Scenarios ──────────────────────────────────────────

const mockConfigs: Record<string, {
  scenarios: Array<{
    id: string;
    name: string;
    description?: string;
    statusCode: number;
    delay: number;
    isTimeout?: boolean;
    responseBody: unknown;
  }>;
  activeScenarioId: string;
}> = <%- JSON.stringify(mockData, null, 2) %>;

// ─── Endpoint → DataSource mapping for URL matching ──────
const endpointMap: Record<string, string> = <%- JSON.stringify(endpointMap, null, 2) %>;

// ─── Initialize ────────────────────────────────────────────
initMock(mockConfigs);
_setEndpointMap(endpointMap);

export { mockConfigs, endpointMap };
