结合 git status 看下最近修复，这些修复才使得生成的项目能够运行起来。
如果我们的目标是：schema 生成的代码就是企业级可运行代码，那么？
- 从第一性原理思考，当前修复的这些问题是怎么发生的？
- 我们 schema 还存在缺陷或者是生成 codegen 策略还存在什么问题导致的么？可以如何解决？
- 解决之后重新生成会不会好？
- 样式转化有问题，数值后面是不是得跟单位，是不是还有更多样式转化问题？
- 页面的高度计算好像出问题了，一直是 0，#root 高度是对的，下面出问题了：父容器（body / #root / .app）没设高度
- 许多 onClick 的属性透传没过去和定义？
- 字体颜色、样式、素材等等，好像都没被正确转换？
- 我看生成的代码里还是存在类型定义是unknown 问题，design_docs/03-tech/schema-type-naming-design.md 这个方案没彻底修复？还是 schema 关于类型的定义仍然存在缺失？如果存在缺失要补齐 schema ，如果是没有彻底修复，从第一性原理分析下为什么，怎么修？
- 从整体分析下：833478e8-17c5-4f1f-b2d2-9ae17012cbcc 项目里的 schema 里的所有内容，是不是都已经表达在代码里了，存在丢失么？为什么？codegen 怎么完善补充这一类内容？
帮我设计个方案，放在 codegen 里，给我看看。