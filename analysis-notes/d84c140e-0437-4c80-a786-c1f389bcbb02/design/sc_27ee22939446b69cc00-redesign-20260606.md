# 登录页重新设计计划（2026-06-06）

> 用户批评当前设计"简陋"，缺乏系统的素材分析方法论。要求结合 `decoration-systems/soft-glow.md` 策略重新设计，注意颜色和素材。

---

## 1. 设计范围

- **项目**：校园社交 App（面向 18-22 在校生 + 23-26 应届生）
- **屏幕**：00-login（登录页）
- **当前状态**：phase = "designed"（但设计目标过低、素材分析不足、装饰系统执行不到位）
- **重新设计原因**：
  1. 设计目标缺乏系统性的素材需求分析（Phase B 未认真执行 `10-material-analysis.md`）
  2. 装饰系统 `soft-glow` 执行不到位（光斑透明度 12%+15% 偏低、位置不正确、未与屏底氛围融合）
  3. 素材真画质量不足（BrandLogo 两圆相遇概念良好，但执行细节未优化：圆心坐标/重叠比例/与装饰系统协同）
  4. 颜色分配未严格遵循 60-30-10 法则（灰阶占比可能 >35%）

---

## 2. 设计目标（Phase B 输出，≥3 个）

### G1: mood-conveyance（P0）
- **statement**：让用户进登录页 0.5 秒感受到"克制温度"调性，区别于纯白冷峻 SaaS 风，降低首次注册防备
- **whyMatters**：用户带"好奇 + 略防备"情绪进屏；视觉先安抚 = 注册转化前置条件；差异化定位（克制温度 vs 即刻多色 / 小红书插画 / Notion 冷峻）的可视化首层
- **impactMode**：mood-conveyance
- **successCriteria**：
  1. "屏底偏暖区（取屏中下 1/3 中心点像素采样）RGB 与 #FFFFFF 距离 ≥ 15pt（暖白米 + 装饰区融合达标，非纯白底）"
  2. "出现 ≥ 2 处 soft-glow 单一族装饰元素（圆形光斑 / 极细几何线，单一族不混杂），核心 alpha ≥ 20%（可分辨色相，非隐形）"
  3. "主色 #5B6CFF 与辅色 #A776FF 仅以'点缀'形态出现，不形成连续 > 30% 屏面积的实色块（避免冷峻 SaaS 调性）"
  4. "整屏灰阶占比 ≤ 35%（按截图主色统计；> 35% 即触发冷峻调）"
- **forbiddenSignals**：
  1. "屏底像素 #FFFFFF 或与之距离 ≤ 5pt"
  2. "装饰核心 alpha < 15% 接近隐形"
  3. "出现直角元素（border-radius < 4px）"
  4. "主色 #5B6CFF 形成连续 > 30% 屏面积的实色块"
  5. "灰阶占比 > 40%（含纯白底 + 灰字 + 灰边）"
  6. "装饰族混杂（光斑 + 几何 + 插画 同时出现）"

### G2: cta-clarity（P0）
- **statement**：让 SubmitBtn 成为首屏唯一主角，用户视线 BrandLogo→FormCard→SubmitBtn 三跳必落于此，与 GetCodeBtn 形成 ≥ 4 权重差
- **whyMatters**：登录核心动作；用户复登求快场景下"找不到主按钮"即流失；medium 紧迫度 + minimal 风格下视觉权重差是硬约束
- **impactMode**：cta-clarity
- **successCriteria**：
  1. "SubmitBtn 与 GetCodeBtn 视觉权重差 ≥ 4（按 weight 公式：色对比 + 尺寸比 + 字重差 + 装饰密度 + 动效 累加）"
  2. "SubmitBtn 视觉高度 ≥ 48px + 字号 ≥ 16px + 字重 ≥ 600 + 上方留白 ≥ 24px"
  3. "SubmitBtn 占 FormCard 内部宽度 100% 且面积占比 ≥ 25%（按 48px / 内容区高度计）"
  4. "SubmitBtn 在 default / hover / pressed / loading / disabled 5 态相邻像素差均 ≥ 3%（每态视觉差异显著可分辨）"
- **forbiddenSignals**：
  1. "SubmitBtn 与 GetCodeBtn 同色 / 同字号 / 同字重（主从混乱）"
  2. "SubmitBtn 在 disabled 态仍显示主色填充"
  3. "SubmitBtn 字号 < 16px 或字重 < 600"
  4. "SubmitBtn 上方留白 < 16px（与字段视觉粘连）"

### G3: trust-signal（P1）
- **statement**：让协议勾选 + 错误反馈以友善温和方式触达，避免合规暴击 / 错误暴击激化用户初见的防备心理
- **whyMatters**：用户进屏带"略防备"；合规红线（必勾协议）若用 native checkbox + 红字暴击会激化防备 → 注册流失；错误暴击让用户怀疑被刁难
- **impactMode**：trust-signal
- **successCriteria**：
  1. "PolicyCheckVisual 视觉为'圆角 ≥ 4px + 主色 #5B6CFF 对勾'，未勾态为细边框 + 透明底，勾选态为主色填充 + 白色对勾，非 native 黑色方框"
  2. "错误态文字色（PhoneError / CredentialError）使用 theme.error #DD4747 (距纯红 #FF0000 ≥ 30pt) + caption 12px 字号软化 + 紧贴 input 下方 4px"
  3. "协议链接（TermsLink / PrivacyLink）使用主色 + textDecoration:underline，与正文 textPrimary 0.88 黑形成可视区分"
  4. "状态切换（勾选 / 错误出现 / 错误消失）有 ≥ 200ms transition，非瞬间 pop"
- **forbiddenSignals**：
  1. "Checkbox 视觉为黑色 native 方块或灰冷无主色"
  2. "错误字色 #FF0000 或与之距离 < 20pt（暴击）"
  3. "错误字号 ≥ 14px（与正文同字号，视觉抢戏）"
  4. "协议链接无可视区分（与正文同色无 underline）"
  5. "状态切换无 transition（瞬间）"

### G4: brand-recognition（P0）
- **statement**：让 BrandLogo 在屏顶撑起品牌识别第一锚点，承载"校园社交 + 现代时尚"双重定位（真画素材非占位虚线，与主色形成视觉联动）
- **whyMatters**：登录页是品牌门面屏；BrandLogo 真画 = 第一印象差异化的载体；23-26 应届对"学生范儿"敏感 = Logo 风格直接决定他们留存判断
- **impactMode**：brand-recognition
- **successCriteria**：
  1. "BrandLogo node.materialProjectId 非空（真调 material-painter 画了 PNG/SVG 素材，非占位虚线 / 文字 placeholder / 空白 div）"
  2. "BrandLogo 视觉边界占首屏面积 ≥ 3%（≥ 100×100 px @ 393×852 viewport，相比 v3 80×80 ≈ 1.4% 是显著台阶）"
  3. "Logo 视觉中含 theme.primary #5B6CFF 或 secondary #A776FF（形态可为描边 / 主体形 / 强调点 / 渐变锚点，非纯黑/纯灰/纯白）"
  4. "Slogan（BrandSlogan）字号 ≥ 16px（body-lg） + 与 BrandLogo 垂直间距 ≤ 12px（视觉成对，非断开）"
- **forbiddenSignals**：
  1. "BrandLogo materialProjectId 为空（仍占位 v8 残留）"
  2. "BrandLogo 视觉占首屏 < 2%"
  3. "Logo 视觉为纯黑 / 纯灰 / 纯白（与主色完全无关）"
  4. "Slogan 与 Logo 间距 > 24px（视觉断开成两组）"
  5. "Logo 形态为单字字标'C'（v3 残留方案，用户已否决字母 logo）"

### G5: state-feedback（P1）
- **statement**：让 loginMode 切换 / 锁定态触发 / 倒计时变化等 5 种业务状态显著可分辨，用户始终知道当前状态 + 还需等多久
- **whyMatters**：登录页业务状态密度高（6 view 字段 + 主流程/locked 双状态机 + 倒计时）；视觉模糊让用户困惑/重试 → 失败率↑ → 后端撞库压力 + 客服成本
- **impactMode**：state-feedback
- **successCriteria**：
  1. "loginMode 切换 (code → password) 前后截图像素差 ≥ 3%：active Tab 与 inactive Tab 字色 / 字重 / 底部 indicator 至少 2 维差异，TabIndicator 位移可见"
  2. "LockedView ↔ NormalFormView 整屏切换截图像素差 ≥ 50%（两个完全不同的子树，含锁形 icon + 倒计时数字 + 找回密码链接）"
  3. "LockedCountdown 倒计时数字字号 ≥ 28px + 字体含 monospace 等宽降级（避免数字变化时 layout shift），辅以柔和而非冷峻的视觉"
  4. "所有状态切换 transition ≥ 200ms（normal 250ms 缓动），非 fast 150ms 一晃而过"
  5. "SubmitBtn loading 态显示 spinner + GetCodeBtn counting 态显示倒计时秒数（'58s'），均与 default 态像素差 ≥ 3%"
- **forbiddenSignals**：
  1. "loginMode 切换无任何视觉变化（像素差 < 1%，TabIndicator 缺失）"
  2. "LockedCountdown 字号 < 24px 或非等宽（数字闪跳）"
  3. "LockedView 视觉冷峻（无任何安抚元素，像 404 错误页）"
  4. "状态切换无 transition（瞬间切换）"
  5. "SubmitBtn loading 与 default 视觉无差（看不出在加载）"

---

## 3. 视觉策略（Phase D 输出）

### weightPyramid
- **hero**: 2（BrandLogo + SubmitBtn）
- **supporting**: 4（FormCard + ModeToggle + LockedView + PolicyRow）
- **tooling**: 7（PhoneInput + CredentialInput + GetCodeBtn + PasswordToggleEye + TermsLink + PrivacyLink + LockedIcon）
- **bit**: 14（PhoneLabel + CredentialLabel + PhoneError + CredentialError + PolicyText + PolicyCheckbox + CheckMark + BrandSlogan + ...）

### decorationSystem
- **family**: `"soft-glow(primary 20% + secondary 15%, 对角配重, 无混杂)"`
- **density**: `"节制"`（2-3 个装饰节点）
- **rationale**: "G1 mood-conveyance 需 soft-glow 光斑氛围 + G4 brand-recognition 需 BrandLogo 主色应用 + G5 state-feedback 需 LockedView 柔和视觉 → soft-glow 单一族可承载所有 goal 需求，与 theme.intent 'minimal+flat' 兼容"

### sixtyThirtyTen
- **sixty**: `"$token:colors.background + surfaceElevated + gray100-300 (60%)"`
- **thirty**: `"$token:colors.primary + secondary (30%)"`
- **ten**: `"$token:colors.error + success + info (10%)"`

### sharedElementsStrategy
1. **BrandLogo**: goals=[G4, G1], strategy="G4 主导(100x100 PNG 真画), G1 共享温度信号", dominantGoal="G4"
2. **SubmitBtn**: goals=[G2, G4], strategy="G2 主导(主色填充 48px), G4 借色(主色应用一致性)", dominantGoal="G2"
3. **PolicyCheckVisual**: goals=[G3, G4], strategy="G3 主导(圆角主色对勾), G4 借色", dominantGoal="G3"

---

## 4. 任务清单（Phase E 输出）

| # | 任务 ID | 任务名称 | 涉及 Goal | 状态 |
|---|---|---|---|---|
| 1 | D-00-login-G1-craft | G1 mood: 屏底+装饰+留白协同 | G1 | pending |
| 2 | D-00-login-G2-craft | G2 cta: SubmitBtn 主角化+邻居弱化 | G2 | pending |
| 3 | D-00-login-G3-craft | G3 trust: checkbox+错误+链接温和 | G3 | pending |
| 4 | D-00-login-G4-craft | G4 brand: BrandLogo 100x100 PNG+主色应用 | G4 | pending |
| 5 | D-00-login-G5-craft | G5 state: Tab+LockedView+countdown 状态反馈 | G5 | pending |
| 6 | D-00-login-self-review | 整屏对账：逐 goal 截图核对 successCriteria | G1-G5 | pending |

---

## 5. 素材需求分析（Phase B/C 输出，参照 `10-material-analysis.md`）

### G1: mood-conveyance
- **需要素材类型**: Background / Decoration
- **素材风格**: soft-glow（从 theme.decorationRules 推导）
- **素材位置**: 背景层 + 右上角溢出 + 左下角溢出
- **素材色彩**: primary 20% + secondary 15%（从 colorRatio 推导）
- **应用节点**: BgSoftGlowPrimary + BgSoftGlowSecondary
- **qualityChecklist**:
  1. "真画 PNG 非 CSS 渐变（materialProjectId 非空）"
  2. "光斑圆形 radial-gradient 双色渐变，透明度 20%+15%，z-index 0"
  3. "与 #FCFCFD 屏底对比度 ≥ 15pt（暖白米与装饰区融合达标）"
  4. "无直角元素（border-radius < 4px）"
  5. "soft-glow 单一族协调"

### G4: brand-recognition
- **需要素材类型**: Brand (Logo)
- **素材风格**: stroke + fill（从 iconSpec 推导）
- **素材位置**: 内容层中心
- **素材色彩**: primary + inverse（从 colorRatio 推导）
- **应用节点**: BrandLogo
- **素材概念**: "两圆相遇"——左圆深=主体『你』，右圆浅=同好『被光照亮的瞬间』。极简几何到无可争议（2 个 ellipse），故事性远强于 C 字标。
- **qualityChecklist**:
  1. "真画 PNG 非占位（materialProjectId 非空）"
  2. "100x100 px 在 #FCFCFD 屏底可视 (与背景对比度 ≥ 30pt)"
  3. "主色 #5B6CFF 或 #A776FF 在 logo 内可见"
  4. "无字母 'C' 字标（用户已否决）"
  5. "圆角 ≥ 4px 无直角元素"
  6. "soft-glow 装饰族协调（与 BgSoftGlowPrimary 主色呼应）"

### G5: state-feedback
- **需要素材类型**: Icon (Lock Icon)
- **素材风格**: stroke（从 iconSpec 推导）
- **素材位置**: LockedView 内容层中心
- **素材色彩**: warning + textSecondary（从 colorRatio 推导）
- **应用节点**: LockedIcon
- **qualityChecklist**:
  1. "真画 PNG/SVG 非占位（materialProjectId 非空）"
  2. "64x64 px 在 LockedView 可视"
  3. "warning #F7A94C 或 secondary #A776FF 在 icon 内可见"
  4. "无冷峻视觉（避免黑色/纯灰）"
  5. "与 LockedView 柔和视觉协调"

---

## 6. 执行备注

1. **严格遵循 `decoration-systems/soft-glow.md` 策略**：
   - 光斑形状：圆形 / 椭圆 / 不规则圆润 blob
   - 填充：径向渐变（中心实色 → 边缘透明）
   - 色调：单色或近似色（不混杂多色）
   - 透明度：opacity 0.2-0.6（信任场景更低）
   - 位置：边缘溢出（top/bottom/left/right 负值）/ 卡片背后 / 主角斜后方
   - 多光斑组合：1-2 个光斑（节制），右上角溢出 + 左下角溢出（对角呼应）
   - 光斑色规则：visualConcept 主色调 = primaryLight（蓝紫淡）
   - 与其他装饰的隔离：soft-glow 装饰系统选定后，仅用圆形 / 圆润 blob 装饰，❌ 不能加直线 / 网格 / 几何形状（混系统），❌ 不能加插画 / 纹理 / 有机曲线

2. **颜色严格使用 token 引用（`$token:colors.xxx`）**：
   - 禁止硬编码 #xxx
   - 禁止字符串内嵌 token（`background: "...$token..."` → 渲染失败）
   - 光斑用 `backgroundColor: "$token:colors.primaryLight"` + `backgroundImage: "radial-gradient(circle, currentColor 0%, transparent 70%)"` + `color: "$token:colors.primaryLight"`（currentColor 注入）

3. **素材绘制前必须读对应的 `decoration-systems/*.md`**：
   - G1 装饰节点 → 读 `decoration-systems/soft-glow.md`
   - G4 BrandLogo → 读 `decoration-systems/soft-glow.md`（与屏底装饰族保持单一）
   - G5 LockedIcon → 读 `decoration-systems/soft-glow.md`（柔和光晕衬托）

4. **每 craft 任务执行后必须截图对账**：
   - 用 `scripts/screenshot-screen.mjs` 生成截图
   - 用 Read 工具看图，逐条核对本任务关联的 successCriteria
   - 任一不达 → 回头改 schema → 重新截图对账 → 重新沉淀 case → 直到达标

---

## 7. 下一步

1. **Phase C**: 拆解每个 goal 的 involvedElements + 5 维 changes（读 `03-goal-decomposition.md`）
2. **Phase D**: 跨目标统筹（读 `05-cross-goal-audit.md`）
3. **Phase E**: 派发 craft 任务（写入 `meta.plan`）
4. **Phase F**: 执行 craft 任务（读 `07-craft-execution.md` + 每任务截图对账）
5. **Phase G**: 整屏对账（读 `09-self-review-by-goals.md`）
6. **Phase H**: 移交（写 `meta.design.handover` + `meta.design.summary` + `status.phase = "designed"`）

---

**文件创建时间**：2026-06-06 13:06
**预计完成时间**：2026-06-06 18:00（5 小时，含截图对账 + 迭代）
